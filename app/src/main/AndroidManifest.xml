<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="com.android.vending.BILLING" />
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.InterviewQuestion"
        tools:targetApi="31">
        <activity
            android:name=".views.BuyNowActivity"
            android:exported="false" />
        <activity
            android:name=".views.QuestionTwentyFiveActivity"
            android:exported="false" />
        <activity
            android:name=".views.TipsActivity"
            android:exported="false"
            android:label="@string/title_activity_tips"
            android:theme="@style/Theme.InterviewQuestion" />
        <activity
            android:name=".views.BillingActivity"
            android:exported="false" />
        <activity
            android:name=".views.QuestionAnswerDescriptionActivity"
            android:exported="false" />
        <activity
            android:name=".views.QuestionActivity"
            android:exported="false" />
        <activity
            android:name=".views.DashboardActivity"
            android:exported="true"
            android:theme="@style/Theme.CustomSplashScreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>